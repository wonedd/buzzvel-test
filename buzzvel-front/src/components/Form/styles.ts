import styled from '@emotion/styled';

export const Container = styled.form`
    display: flex;
    flex-direction: column;
    gap: 32px;

    width: 100%;
    max-width: 400px;

    padding: 8px;
`;
